import os
os.system('ls') # list the current files in current directory
print(os.getcwd()) # shows the current working directory
os.chdir('/') # change the current directory to /
print(os.getlogin()) # show the current user name
print(os.cpu_count()) #shows the number of cpus
